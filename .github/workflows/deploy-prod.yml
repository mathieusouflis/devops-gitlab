name: Deploy Production

on:
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: write
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      environment: production
